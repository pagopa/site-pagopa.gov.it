name: "Deploy AWS"

on:
  push:
    branches:
      - master
    paths:
      - 'src/app/**'
      - '.github/workflows/*.yml'
  workflow_dispatch:
    inputs:
      logLevel:
        description: 'Log level'
        required: true
        default: 'warning' 
        type: choice
        options:
        - info
        - warning
        - debug
      environment:
        description: 'Which environment to update.'
        type: choice
        default: 'prod'
        required: true 
        options:
        - prod
env:
 AWS_REGION: eu-south-1
 IAM_ROLE: arn:aws:iam::476761402556:role/GitHubActionDeploy
 JEKYLL_ENV: production
 AWS_BUCKET: pagopa.gov.it
 AWS_CLOUDFRONT_ID: EQ48YU0B7VEBZ

jobs:
  deploy:
    name: "deploy"
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      
      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
          role-to-assume: ${{ env.IAM_ROLE }}
          aws-region: ${{ env.AWS_REGION }}
      - name: Bundle
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '2.7'
          bundler-cache: true
      - name: Build
        run: bundle exec jekyll build --config _config.yml
        env:
          JEKYLL_ENV: production
      - name: Check 
        run: | 
          pwd
          ls -lrt
      - name: "Deploy to AWS S3"
        run: aws s3 sync --dryrun ./_site/ s3://${{ env.AWS_BUCKET }} --acl public-read --delete 
      - name: "Create AWS Cloudfront Invalidation"
        run: aws cloudfront create-invalidation --distribution-id ${{ env.AWS_CLOUDFRONT_ID }} --paths "/*"
